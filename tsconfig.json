{
  // Raiz vazia que só aponta para os dois projetos reais.
  // O app e os arquivos de ferramenta (vite.config.ts) rodam em ambientes
  // diferentes — navegador e Node — e precisam de configurações diferentes.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
